# Loaded automatically by `npm run dev` (Vite mode=development).
# Points the UI at the hosted API so you are not stuck on localhost:3000/api.
#
# Local backend instead: create frontend/.env.development.local (gitignored) with:
#   VITE_USE_LOCAL_API=1
VITE_API_BASE=https://remix-fnd.onrender.com
