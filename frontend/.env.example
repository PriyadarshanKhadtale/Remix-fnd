# Copy to .env.local (gitignored). No trailing slash on URLs.
#
# Hosted backend (Render — lite mode by default):
#   https://remix-fnd.onrender.com
# Do NOT add /api — routes are /detect, /health (not /api/detect).
# VITE_API_BASE=https://remix-fnd.onrender.com
#
# Local full stack: leave VITE_API_BASE unset; npm run dev proxies /api -> http://127.0.0.1:8000

# VITE_API_BASE=
