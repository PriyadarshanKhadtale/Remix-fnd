# Copy to .env.local (gitignored). No trailing slash.
# Hosted lite API (Render free tier default):
# VITE_API_BASE=https://remix-fnd.onrender.com
#
# Local full backend (default in dev is Vite proxy /api -> :8000):
# leave unset when using npm run dev + backend on 8000

# VITE_API_BASE=
